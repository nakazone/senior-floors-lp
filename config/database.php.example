<?php
/**
 * Database Configuration - EXEMPLO
 * 
 * INSTRUÇÕES:
 * 1. Copie este arquivo para database.php
 * 2. Preencha com suas credenciais do Hostinger
 * 3. NUNCA commite o database.php real (já está no .gitignore)
 */

// Configurações do banco de dados
define('DB_HOST', 'localhost');
define('DB_NAME', 'SEU_USUARIO_senior_floors_db'); // ⚠️ Nome COMPLETO do banco (com prefixo)
define('DB_USER', 'SEU_USUARIO_senior_floors_user'); // ⚠️ Nome COMPLETO do usuário (com prefixo)
define('DB_PASS', 'SUA_SENHA_AQUI'); // ⚠️ Senha que você criou

// Exemplo real (substitua pelos seus valores):
// define('DB_NAME', 'u123456789_senior_floors_db');
// define('DB_USER', 'u123456789_senior_user');
// define('DB_PASS', 'MinhaSenh@Segura123!');

/**
 * Função para conectar ao banco de dados
 * @return PDO|null Retorna conexão PDO ou null em caso de erro
 */
function getDBConnection() {
    static $pdo = null;
    
    if ($pdo === null) {
        try {
            $dsn = "mysql:host=" . DB_HOST . ";dbname=" . DB_NAME . ";charset=" . DB_CHARSET;
            $options = [
                PDO::ATTR_ERRMODE            => PDO::ERRMODE_EXCEPTION,
                PDO::ATTR_DEFAULT_FETCH_MODE => PDO::FETCH_ASSOC,
                PDO::ATTR_EMULATE_PREPARES   => false,
            ];
            
            $pdo = new PDO($dsn, DB_USER, DB_PASS, $options);
        } catch (PDOException $e) {
            error_log("Database connection error: " . $e->getMessage());
            return null;
        }
    }
    
    return $pdo;
}

/**
 * Verifica se o banco de dados está configurado
 * @return bool
 */
function isDatabaseConfigured() {
    return !empty(DB_USER) && DB_USER !== 'seu_usuario' && 
           !empty(DB_PASS) && DB_PASS !== 'sua_senha' &&
           !empty(DB_NAME) && DB_NAME !== 'senior_floors_db';
}
