name: Deploy to Hostinger (SSH) - Fixed

# Deploy automático: qualquer push na branch main dispara este workflow (SSH).
on:
  push:
    branches:
      - main
  workflow_dispatch: # Permite rodar manualmente em Actions

jobs:
  deploy:
    name: Deploy to Hostinger via SSH
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Validate Secrets
        run: |
          if [ -z "${{ secrets.HOSTINGER_SSH_HOST }}" ]; then
            echo "❌ Error: HOSTINGER_SSH_HOST secret is not set"
            exit 1
          fi
          if [ -z "${{ secrets.HOSTINGER_SSH_USER }}" ]; then
            echo "❌ Error: HOSTINGER_SSH_USER secret is not set"
            exit 1
          fi
          if [ -z "${{ secrets.HOSTINGER_SSH_KEY }}" ]; then
            echo "❌ Error: HOSTINGER_SSH_KEY secret is not set"
            exit 1
          fi
          if [ -z "${{ secrets.HOSTINGER_DOMAIN }}" ]; then
            echo "❌ Error: HOSTINGER_DOMAIN secret is not set"
            exit 1
          fi
          echo "✅ All required secrets are configured"
      
      - name: Setup SSH key
        run: |
          # Create .ssh directory with proper permissions
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          
          # Write SSH key (restore newlines if key was pasted with \n literal in GitHub Secrets)
          echo "${{ secrets.HOSTINGER_SSH_KEY }}" | sed 's/\\n/\n/g' > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          
          # Add host to known_hosts
          ssh-keyscan -H ${{ secrets.HOSTINGER_SSH_HOST }} -p ${{ secrets.HOSTINGER_SSH_PORT || 22 }} >> ~/.ssh/known_hosts 2>/dev/null || true
          
          # Verify SSH key was created and looks valid
          if [ ! -f ~/.ssh/id_rsa ]; then
            echo "❌ Error: Failed to create SSH key file"
            exit 1
          fi
          if ! grep -q "BEGIN.*PRIVATE KEY" ~/.ssh/id_rsa; then
            echo "❌ Error: SSH key file does not look valid (missing BEGIN PRIVATE KEY)."
            echo "   In GitHub Secrets, paste the full key including -----BEGIN ... ----- and -----END ... -----"
            echo "   Use one line per line, or if pasted as one line use literal \\n between lines."
            exit 1
          fi
          echo "✅ SSH key setup completed"
      
      - name: Test SSH Connection
        run: |
          ssh -i ~/.ssh/id_rsa \
            -o StrictHostKeyChecking=no \
            -o UserKnownHostsFile=~/.ssh/known_hosts \
            -p ${{ secrets.HOSTINGER_SSH_PORT || 22 }} \
            ${{ secrets.HOSTINGER_SSH_USER }}@${{ secrets.HOSTINGER_SSH_HOST }} \
            "echo 'SSH connection successful'" || {
              echo "❌ SSH connection test failed"
              exit 1
            }
      
      - name: Remove files that shouldn't be deployed
        run: |
          rm -rf .git .github node_modules
          rm -f .DS_Store *.log leads.csv
          rm -f test-*.html test-*.php debug-*.html
          # Keep config/database.php and admin-config.php (they may be needed)
          # Keep PHPMailer if it exists
      
      - name: Deploy via SCP
        run: |
          scp -r \
            -i ~/.ssh/id_rsa \
            -P ${{ secrets.HOSTINGER_SSH_PORT || 22 }} \
            -o StrictHostKeyChecking=no \
            -o UserKnownHostsFile=~/.ssh/known_hosts \
            ./* ${{ secrets.HOSTINGER_SSH_USER }}@${{ secrets.HOSTINGER_SSH_HOST }}:/home/${{ secrets.HOSTINGER_SSH_USER }}/domains/${{ secrets.HOSTINGER_DOMAIN }}/public_html/
      
      - name: Cleanup SSH key
        if: always()
        run: |
          rm -f ~/.ssh/id_rsa
          rm -f ~/.ssh/known_hosts
