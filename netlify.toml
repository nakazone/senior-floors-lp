# Netlify deployment configuration
[build]
  publish = "."
  command = "echo 'No build step needed for static site'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
